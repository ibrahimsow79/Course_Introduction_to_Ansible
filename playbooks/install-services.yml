# install-services.yml

---
  - hosts: loadbalancers
    become: true
    tasks:
      - name: installing Apache
        yum: name=httpd state=present

  - hosts: webservers
    become: true
    tasks:
      - name: installing services
        yum:
          name:
            - httpd
            - php
          state: present